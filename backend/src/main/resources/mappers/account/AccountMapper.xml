<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tickle_moa.backend.account.query.mapper.AccountMapper">

    <select id="findByUserId" resultType="com.tickle_moa.backend.account.query.dto.response.AccountDTO">
        SELECT * FROM accounts WHERE user_id = #{userId}
    </select>

</mapper>
