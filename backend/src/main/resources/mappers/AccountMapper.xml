<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.backend.mapper.AccountMapper">

    <select id="findByUserId" resultType="com.ohgiraffers.backend.model.Account">
        SELECT * FROM accounts WHERE user_id = #{userId}
    </select>

    <insert id="insertAccount">
        INSERT INTO accounts (user_id, name, balance)
        VALUES (#{userId}, #{name}, #{balance})
    </insert>

    <delete id="deleteById">
        DELETE FROM accounts WHERE account_id = #{accountId}
    </delete>

</mapper>
